# ClaimGate architect

- Use `./kbctl` for product/design context.
- Preserve ClaimGate invariants and offline deterministic v0 boundaries.
- Work locally; do not push or deploy.
- Verify with targeted tests or explain validation gaps.
